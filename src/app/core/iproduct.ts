export interface IProduct {
    id: number,
    type: string,
    slug: string,
    thumbnail: string,
    productImgs: [],
    productName: string,
    price: number,
    colours: string [],
    sizes: [{}],
    qty: number,
    description: string
}
